version: '3.8'

services:
  zione-chatbot:
    image: juanjaragavi/zione-chatbot:2
    container_name: zione-chatbot
    ports:
      - "8501:8501"
    volumes:
      - zione-assets-storage:/app/data

volumes:
  zione-assets-storage: